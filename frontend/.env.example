# Frontend (Vite) example environment
# Copy to .env and edit values. Do NOT commit secrets.

# Auth mode: 'dev' or 'cognito'
VITE_AUTH_MODE=cognito

# Cognito hosted UI domain (no https://). Example:
# my-app.auth.eu-west-1.amazoncognito.com
VITE_COGNITO_DOMAIN=your-cognito-domain.auth.eu-west-1.amazoncognito.com

# Public Cognito client ID (non-secret)
VITE_COGNITO_CLIENT_ID=2b10tfhn1k9pq9rr5f6k14usc3

# Backend endpoint that will exchange auth code for tokens
VITE_BACKEND_TOKEN_CALLBACK=http://localhost:3000/auth/token

# 'server' (backend exchanges code) or 'pkce' (SPA handles PKCE)
VITE_AUTH_FLOW=server
